[tool.poetry]
name = "tt2"
version = "0.1.0"
description = ""
authors = ["YouGuessedMyName <ivo@melse.info>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
